const chai = require('chai');
const expect = chai.expect;
const binarySearchTree = require('./binary_search_tree');

describe('data_structures/binary_search_tree', () => {
	it('empty structure works', () => {
	//	expect(binarySearchTree).to.exist;
	});
});

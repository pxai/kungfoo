const chai = require('chai');
const twoSum = require('./twoSum');
const expect = chai.expect;

describe('TwoSum problem', () =>{
  it('should return', () => {
      expect(twoSum()).to.equal('yeah');
  })
})
